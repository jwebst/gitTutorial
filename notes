Tutorial on git by Michael Beyeler
2017 April 18

git init
git add <file>
git commit -m "<comment>"
git status
git log
git branch

(create remote respository on GitHub)
   copy URL

git remote add origin <URL>
git push origin master

git checkout <hash>
git checkout -b '<branch name>'

git checkout master


git clone <repo>
